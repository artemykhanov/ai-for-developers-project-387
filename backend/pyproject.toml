[project]
name = "calking-backend"
version = "0.1.0"
description = "FastAPI backend for CalKing"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.124.0",
]

[tool.uv]
package = false

[tool.fastapi]
entrypoint = "app.main:app"
